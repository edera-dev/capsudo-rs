[package]
name = "capsudo-transport"
description = "Transport abstraction for capsudo: local Unix sockets today, cross-zone IDM tomorrow"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true

[features]
# A throwaway cross-zone transport stub built on the multiplexer, standing in
# for a real Edera Protect IDM channel with TCP. Off by default; not for shipping
# (the real integration lives in Protect's zone agent).
fakeidm = []

[dependencies]
capsudo-proto.workspace = true
thiserror.workspace = true
async-trait.workspace = true
tokio.workspace = true
nix.workspace = true
libc.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["test-util"] }
